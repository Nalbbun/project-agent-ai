apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-general
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-general
  template:
    metadata:
      labels:
        app: vllm-general
    spec:
      containers:
        - name: server
          image: vllm/vllm-openai:latest
          args:
            - --model
            - Qwen/Qwen2.5-3B-Instruct
            - --served-model-name
            - qwen-general
            - --enable-lora
            - --host
            - 0.0.0.0
            - --port
            - "8000"
          ports:
            - containerPort: 8000
          env:
            - name: HUGGING_FACE_HUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token
                  key: token
          volumeMounts:
            - name: adapters
              mountPath: /models/adapters
      volumes:
        - name: adapters
          persistentVolumeClaim:
            claimName: agent-adapters-pvc
